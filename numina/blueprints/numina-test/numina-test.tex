\begin{theorem}[de Bruijn--Erdős theorem]
Let \(G=(V,E)\) be a graph, possibly infinite, and let \(k\ge 1\) be an
integer. Suppose that every finite subgraph of \(G\) is \(k\)-colorable.
Then \(G\) itself is \(k\)-colorable.
\end{theorem}